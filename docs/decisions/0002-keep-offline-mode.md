# ADR-0002：保留离线模式

- 状态：已采纳
- 日期：2026-07-23

## 背景

模型服务可能因密钥、限流、网络、费用或输出格式失败。只提供在线模式会让演示、测试和 CI 都依赖外部状态。

## 决策

保留 `mode=offline`：规则抽取、哈希检索和模板报告组成完整闭环。Agent 模式失败时记录原因并降级，而不是把一次模型故障扩大成整个任务失败。

## 后果

CI 可以零密钥运行，用户可以对比规则基线和模型方案。代价是需要维护两套实现，并明确显示本次执行使用了 `llm`、`rules` 还是 `fallback`，避免用户误以为降级结果来自模型。
