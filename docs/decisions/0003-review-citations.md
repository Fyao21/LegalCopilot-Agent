# ADR-0003：引用必须经过审核

- 状态：已采纳
- 日期：2026-07-23

## 背景

大模型擅长生成语言，但可能编造法条名称、条号或内容。法律场景无法接受只有自然语言、不能回查来源的引用。

## 决策

检索结果必须包含数据库 `article_id`。审核节点先核对 ID、法律名称、条号和正文是否与数据库一致，再进行可选的 LLM 语义支撑审核。报告只使用审核通过的引用。

## 后果

引用可以回查，篡改内容会被拒绝，模型不能创造数据库外法条。该设计保证“引用真实”，但不自动保证法规现行有效或结论正确，因此页面始终保留人工复核声明。
