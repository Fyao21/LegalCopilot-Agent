# ADR-0004：工作流使用 LangGraph

- 状态：已采纳
- 日期：2026-07-23

## 背景

案件分析包含抽取、检索、审核、重试和写作。普通顺序函数能够完成第一版，但条件重试、节点状态和未来人工审批会让控制流逐渐复杂。

## 决策

使用 LangGraph 的显式状态图描述节点与条件边。每个节点只读取和返回受控状态，并把公开动作摘要、耗时和进度持久化。

## 后果

控制流可视、节点可测试、失败位置可定位。代价是小型流程引入额外框架。若未来流程始终保持线性且不需要状态恢复，可重新评估是否回退为普通服务编排。
