# 律镜 Legal Copilot Agent 产品说明书

## 1. 产品信息

- 产品名称：律镜 Legal Copilot Agent
- 当前版本：1.2.0（第八周 Agent 运行监控与费用面板版）
- 目标用户：希望快速整理合同纠纷或劳动争议材料的个人、法律科技学习者和项目评审者
- 产品性质：法律 AI 工程演示项目
- 风险声明：所有输出仅供技术演示，不构成法律意见，法规必须以权威来源的现行有效文本为准

## 2. 用户问题

用户拿到案件材料时，通常需要完成三件事：从长文本中整理事实；找到可能相关的法律条文；将事实、争议和依据组织为便于阅读的分析。直接让大模型回答容易出现虚构条文、引用无法回查和执行过程不可见的问题。

本产品通过“结构化提取、知识库检索、引用审核、报告生成”四个节点解决这些问题。模型只负责适合模型的理解和表达，条文真实性由数据库 ID 和服务端校验负责。

## 3. 当前版本范围

### 已支持

1. 上传 TXT、DOCX、带文本层的 PDF，或只输入案件问题。
2. 提取案件类型、当事人、事实、诉求、争议焦点、信息缺口和待确认问题。
3. 支持劳动、合同、食品安全、消费者权益和侵权责任等教学场景。
4. 使用关键词分数与语义分数进行混合检索。
5. 使用 LangGraph 执行案件分析、法规检索、引用审核和报告生成。
6. 引用审核会校验条文 ID、法律名称、条号和正文是否与数据库一致。
7. Agent 模式可以调用 OpenAI 兼容的模型接口；模型不可用时自动回退到规则方案。
8. 离线模式不需要模型 API Key，能够完整生成 Markdown 报告。
9. 检索失败时最多补充检索两次，之后生成明确标注的低置信度报告。
10. 提供任务状态、节点轨迹、引用列表和报告读取接口。
11. 提供 React + TypeScript 浏览器界面，支持拖拽上传、示例问题、进度轮询和错误提示。
12. Agent 工作流通过 FastAPI 后台任务执行，创建请求立即返回 `queued`。
13. 只有人工审批通过的报告版本支持 Markdown 和中文 PDF 下载。
14. 上传文件限制为 TXT、DOCX、PDF 和 10 MB，并校验扩展名、MIME、空文件与解析超时。
15. 请求日志包含 request ID、路径、状态码和耗时，不记录密钥与案件全文。
16. 提供前后端 Dockerfile 和 Docker Compose 一键启动配置。
17. 提供 24 条脱敏评测集、抽取/检索/系统指标和三种离线检索对照。
18. 运行状态明确展示实际执行引擎和模型，区分 LLM、离线规则与 Agent 降级。
19. 提供 Ruff、mypy、GitHub Actions 和不访问真实模型的 CI 测试入口。
20. 提供技术决策记录、演示脚本、简历描述和面试问答。
21. LLM 返回空案件类型时先进行一次结构化输出修复；仍为空则回退规则识别，页面不再显示空白类型。
22. 前端提供独立知识库管理页面，支持手工填写或从 TXT 导入单条正文、字段校验、重复提示、保存成功反馈和法规数量刷新。
23. 支持一次选择最多 200 个四行格式 UTF-8 TXT，浏览器预解析，后端重新校验并逐文件返回新增、重复或格式错误。
24. 提供 200 个跨劳动、消费、资源环境、广告、竞争、价格、计量、药品、电商和食品安全领域的示例文件及来源清单。
25. LLM 对短问题返回“未识别”时，保留模型抽取的信息并由本地分类器补全类型，避免整个结果退化为空。
26. 在线 Embedding 瞬时失败时在同一检索节点重试一次；连续失败才回退 hash，并在轨迹中显示具体原因。
27. 对 LLM 把 `suggestions`、`evidence_gaps` 等数组错误返回为字符串的常见情况进行安全归一化，减少无意义的模板回退。
28. 提供可按分类筛选的随机案件问题接口，首页展示三条随机示例并支持“换一批”，接口不可用时保留本地兜底问题。
29. 随机示例支持 `brief` 与 `detailed` 两种详细程度；详细案情包含时间、主体、经过、损害结果、证据、对方态度和诉求。
30. 知识库管理页提供“Agent 整理”，使用在线 LLM 从任意排版的单条法规 TXT 中提取名称、条号、来源和正文，并生成标准四行 TXT 草稿。
31. 智能整理不会自动写数据库；缺字段或检测到多条内容时阻止直接导入，所有结果都要求人工核对。
32. 案件要素存在关键缺口时，LangGraph 使用 `interrupt` 暂停并持久化 checkpoint，任务进入 `waiting_for_user`。
33. 用户可以通过待办接口或前端追问卡片回答，系统在同一 `run_id` 上恢复，并继续完成检索、引用审核和当前最终报告。
34. 回答接口要求 `Idempotency-Key` 和 `state_version`，重复请求不会重复恢复，过期页面提交返回 409。
35. 最多追问三轮、每轮最多五问；达到上限后保留证据缺口并继续生成低置信度报告，避免无限等待。
36. 页面刷新后使用已保存的 run ID 重新查询待办；服务应用上下文重启后仍能从 SQLite checkpoint 恢复。
37. 报告生成后进入 `waiting_for_approval`，前端展示完整草稿、引用、证据缺口和审批卡片。
38. 审核人可以批准、要求修改或驳回；要求修改会生成下一版本并再次进入审批，驳回会终止发布。
39. `report_versions` 追加保存每版标题、Markdown、事实快照、引用快照、修改摘要和状态，旧版本不会覆盖。
40. `agent_approvals` 保存每次审批动作、意见、审核人、状态版本、幂等键和时间。
41. 审批接口使用 `expected_state_version` 防止并发覆盖，使用 `Idempotency-Key` 防止网络重试重复执行。
42. 前端提供版本列表与统一 diff；批准前不会显示下载按钮，后端导出接口也会返回 409。
43. 用户可以为法规搜索、同步案件分析和 Agent 任务传入可选 `as_of_date`；留空时按当前日期检索。
44. `laws` 与 `law_versions` 分开保存法规实体、版本、生效/失效日期、状态和替代关系。
45. 检索先按半开区间 `[effective_from, effective_to)` 过滤，再做关键词与向量召回，避免无效版本占用 Top-K。
46. 引用、运行记录和报告快照保存 `law_version_id`，报告展示版本、效力状态和适用区间。
47. 前端提供案件日期选择器；运行轨迹、引用卡片和详情抽屉可区分现行版本与历史废止版本。

### 当前不支持

1. 扫描版 PDF OCR。
2. 跨进程持久任务队列；等待用户回答的任务已经持久化，但正在执行检索或写报告时进程退出仍不会自动恢复。
3. 全量权威法规库、法规版本自动更新和官方来源定期复核。
4. 用户账号、权限、计费和多租户。
5. 真实法律意见、胜诉预测或自动诉讼决策。
6. 知识库管理员认证、角色权限和写入审计；管理页当前只适合本地演示。

## 4. 两种运行模式

### Offline 模式

请求参数 `mode=offline`。使用确定性规则提取案件，使用中文二元词哈希向量和关键词混合检索，使用模板生成报告。

适用场景：本地开发、自动测试、没有 API Key、演示降级能力。

### Agent 模式

请求参数 `mode=agent`。当 `.env` 中 `OFFLINE_MODE=false` 且配置了模型密钥时，案件分析、语义审核和报告写作使用 LLM；Embedding 使用独立的 provider、Key、地址和模型。在线向量发生瞬时错误时先重试一次，连续失败才降级到哈希，并在节点轨迹中记录实际 provider 和回退原因。

如果模型调用超时、输出不符合 Schema 或没有密钥，系统记录回退原因并继续使用规则或模板，不让单次模型故障导致整个服务退出。

## 5. 核心工作流

```text
analyze_case
    ├─ 信息不足且未达三轮上限 → clarify_user → interrupt
    │                                  ↓ 用户回答
    │                              Command(resume)
    │                                  ↓
    │                              analyze_case
    ↓ 信息充分或达到上限
retrieve_laws
    ↓
review_citations
    ├─ 有可信引用 → write_report → approve_report → interrupt
    ├─ 无可信引用且重试次数 < 2 → retry_retrieval → retrieve_laws
    └─ 无可信引用且达到上限 → 低置信度 write_report → approve_report

approve_report
    ├─ approve → completed
    ├─ request_changes → revise_report → approve_report
    └─ reject → rejected
```

### 节点职责

- `analyze_case`：抽取结构化案件事实；LLM 失败时回退规则。
- `clarify_user`：将问题作为 JSON 可序列化 interrupt 暂停；回答后合并补充事实并回到要素提取。
- `retrieve_laws`：先按案件日期或当前日期筛选有效版本，再融合关键词与语义相似度。
- `review_citations`：先做数据库一致性校验，再做可选语义支撑审核。
- `retry_retrieval`：增加案件类型、法律责任和请求权基础等检索信息。
- `write_report`：只使用案件事实和审核通过的引用生成第一版报告草稿。
- `approve_report`：用第二个 LangGraph interrupt 暂停并等待人工批准、修改或驳回。
- `revise_report`：根据审批意见生成下一报告版本；离线模式保守记录修改要求，Agent 模式可调用 LLM 修订。

## 6. 主要数据

### 法规

法规实体保存在 `laws`，具体版本保存在 `law_versions`，包括版本标签、生效日、失效日、状态、来源和替代版本 ID。`legal_articles.law_version_id` 将条文绑定到精确版本。内置 JSONL 保存高频教学参考，启动时增量补充并回填当前版本；另有《合同法》第九十四条到《民法典》第五百六十三条的前后时点演示数据。不同 Embedding provider/model 的向量单独存放，正文不变时不会重复计算。

### Agent 任务

任务保存原始问题、文件文本、运行模式、`as_of_date`、状态、进度、重试次数、追问轮次、状态版本、当前/已批准报告版本、checkpoint thread ID、案件要素、节点轨迹、报告、错误和模型名称。`agent_clarifications` 逐轮保存问题与回答；`report_versions` 保存不可覆盖的报告快照；`agent_approvals` 保存审批决定和意见；LangGraph checkpoint 表与业务表共用当前 SQLite 文件。

### 审核引用

每条引用保存综合分数、关键词分数、语义分数、`law_version_id`、版本标签、效力区间、审核状态、审核理由和是否通过。真实性审核同时校验条文与版本绑定；报告只展示通过审核的条文。

## 7. 主要接口

- `GET /health`：健康检查。
- `GET /api/v1/examples/questions/random`：随机生成 1～10 个案件问题，可按分类筛选。
- `POST /api/v1/articles/search`：直接检索法规。
- `GET /api/v1/articles/{article_id}`：回查法规原文。
- `POST /api/v1/knowledge/articles`：新增单条法规、判重并同步生成检索向量。
- `POST /api/v1/knowledge/articles/batch`：批量处理最多 200 个四行格式 TXT，返回逐文件结果。
- `POST /api/v1/knowledge/articles/normalize`：用在线 LLM 整理一个非标准 TXT，返回可下载、可核对的标准四行草稿，不写数据库。
- `POST /api/v1/cases`：第一周兼容的同步分析接口。
- `POST /api/v1/runs`：执行完整 Agent 工作流。
- `GET /api/v1/runs/{run_id}`：读取状态和节点轨迹。
- `GET /api/v1/runs/{run_id}/pending-action`：读取当前追问或报告审批待办和状态版本。
- `POST /api/v1/runs/{run_id}/clarifications`：幂等提交本轮回答并恢复工作流。
- `POST /api/v1/runs/{run_id}/approvals`：幂等提交批准、要求修改或驳回。
- `GET /api/v1/runs/{run_id}/report-versions`：读取所有报告版本摘要。
- `GET /api/v1/runs/{run_id}/report-versions/{version}`：读取指定版本及与上一版的差异。
- `GET /api/v1/runs/{run_id}/citations`：读取审核结果。
- `GET /api/v1/runs/{run_id}/report`：读取 Markdown 报告。
- `POST /api/v1/runs/{run_id}/retry`：重试失败任务并创建新任务。

完整请求与响应见 `docs/openapi.json`。

## 8. 成功标准

第二周版本达到以下标准才算完成：

1. 无 API Key 时完整工作流成功结束。
2. 合同和劳动问题能够识别正确领域。
3. 每条报告引用都能通过 `article_id` 回查数据库原文。
4. 篡改引用正文后审核节点将其拒绝。
5. 模型输出非法 JSON 时只修复一次，仍失败则安全回退。
6. 空知识库最多重试两次，不发生无限循环。
7. 所有自动测试通过。

## 9. 第三周交付结果

第三周已经交付 React 页面、后台任务、1.5 秒进度轮询、引用抽屉、Markdown/PDF 下载、上传安全限制、结构化请求日志和 Docker 配置。现有 Agent API 路径保持兼容，创建响应从同步完成调整为标准的 `queued`。

当前 Dockerfile 与 Compose 已编写；由于开发机未安装 Docker，本次无法执行容器构建，安装 Docker Desktop 后应按 `SELF_TEST.md` 完成最终运行验收。

## 10. 第四周交付结果

第四周新增 `eval/dataset.jsonl` 中 24 条合同与劳动争议脱敏样例，标注案件类型、事实关键词、争议焦点关键词和相关 article ID。`eval/run_eval.py` 固定 Top-K=5，输出 JSON 与 Markdown 结果，包含案件类型准确率、关键词覆盖率、Recall@5、MRR、Hit@K、Precision@5、平均/P95 耗时和工作流成功率。

首次规则基线案件类型准确率为 75%。评测发现“供应商未交付”“入职未签合同”等表达不在原词表，补充领域词后同一数据集达到 100%。最初 10 条教学法规上，纯关键词 Recall@5 为 1.0000，哈希语义为 0.8403，哈希混合为 0.8264；扩充到 36 条后分别为 0.7639、0.6458、0.6806。候选增加后指标下降，说明需要复核相关条文标注并继续改进检索，而不能把小数据高分外推到更大知识库。

在线 Embedding 组已经由评测器支持，但当前没有独立 Embedding 服务，实际结果标记为 `skipped`。它不能用 DeepSeek Chat 配置替代，也不会伪造指标。

工程质量交付包括：

1. 自测入口强制离线并清除模型 Key，避免本地测试或 CI 产生真实费用；
2. 自动测试扩展到 84 项，覆盖详细案件示例、损坏 PDF、重复提交、提示/SQL 注入文本、单条与批量知识库写入、200 条样例结构、非标准 TXT 整理、缺字段拦截、在线 Embedding 重试/降级、真实余弦计算、LLM 类型补全、数组归一化、SQLite 锁重试、追问暂停/恢复、报告审批、法规日期边界、版本引用持久化、带日期引用的模型报告生成，以及 Trace、监控聚合、人民币费用估算和遥测隐私；
3. Ruff 统一格式和静态检查，mypy 检查核心服务与评测代码；
4. GitHub Actions 分别执行后端质量门禁、离线评测和前端生产构建；
5. 状态接口返回 `execution_engine` 和 `model`，前端直接显示是否真实使用模型；
6. README、ADR、演示脚本和求职面试材料可以独立说明项目。

## 11. 第四周发布标准

- 评测命令在没有 API Key 时可重复运行；
- 指标同时保留效果、延迟、限制和未运行项，不只展示最佳方案；
- 测试和 CI 不访问真实模型服务；
- README 中的量化结论均能在 `eval/results/latest.json` 回查；
- 公开仓库不包含 `.env`、数据库、虚拟环境、依赖目录和上传材料；
- 演示视频需由项目作者按 `docs/DEMO_SCRIPT.md` 录制，录制前检查隐私信息；
- Docker 与在线 Embedding 在实际环境验证前继续标记为待验收。

## 12. 第五周交付结果

第五周独立交付“追问—回答—恢复—当前最终报告”完整闭环。合同交付场景缺少交货日期或履行期限时，Agent 不再带着缺失事实直接生成报告，而是进入 `waiting_for_user`。前端显示问题，用户回答或选择“暂不清楚”后，系统用 `Command(resume=...)` 恢复原 LangGraph thread，继续检索、审核和报告生成。

在 0.9.0 历史版本中，这项功能不依赖第六周审批，恢复后可直接下载当时的当前最终报告。升级到 1.0.0 后，第五周的追问闭环仍然完整，但生成结果会继续进入第六周审批；只有批准版本可以作为新的最终报告下载。

## 13. 第六周交付结果

第六周独立交付“草稿—审批—修改/驳回—发布”完整闭环。任意不触发追问的案件也能单独演示：报告完成后状态为 `waiting_for_approval`，审核人可以查看草稿、引用和证据缺口。

- 批准：原版本标记为 `approved`，任务进入 `completed`，开放 Markdown/PDF 下载；
- 要求修改：原版本标记为 `superseded`，Agent 生成下一版本并再次等待审批；
- 驳回：版本标记为 `rejected`，任务进入终态 `rejected`，仍可查看审计草稿但不能导出；
- 并发：审批时用条件更新校验 `state_version`，两个页面只有第一个请求能够改变状态；
- 审计：每版内容、事实、引用、修改摘要以及每次审批意见均追加保存。

该功能不依赖第七周法规效力日期即可运行、测试和演示。

## 14. 第七周交付结果

第七周独立交付“案件日期—法规效力过滤—版本引用—报告展示”闭环。用户在前端选择案件发生日期，或者在 API 传入 `as_of_date`；系统在召回前过滤当时无效的版本，再执行原有混合检索和引用审核。

- 日期区间采用 `[effective_from, effective_to)`：生效日当天可用，失效日当天不可用；
- 2020-12-31 的解除合同问题返回《合同法》第九十四条“1999年施行版本”；
- 2021-01-01 以及留空按当前日期检索时返回《民法典》第五百六十三条现行版本；
- 历史版本仍可按历史时点引用，但在界面标为“已废止·历史时点适用”，不会被误认为当前有效；
- `case_runs`、`agent_runs` 保存用户选择日期，检索日志和 Agent 引用保存精确 `law_version_id`；
- 报告正文、引用快照和前端详情都显示版本标签、效力状态和适用区间。

该功能使用现有 SQLite、LangGraph 与检索服务即可完整演示，不依赖第八周监控功能。

## 15. 第八周交付结果

第八周独立交付“Trace ID—节点 Span—指标聚合—监控页面”闭环。创建任务时生成 32 位 Trace ID，状态接口同步返回监控地址；工作流为案件抽取、LLM、Embedding、检索、引用审核、报告生成、追问/审批恢复和重试记录脱敏 Span。

- 单次监控返回节点耗时、provider/model、输入/输出 Token、估算费用、重试次数、降级原因和最慢节点；
- 概览接口按 1～90 天聚合运行数、成功率、降级率、平均/P95 耗时、Token、费用和各节点延迟；
- 前端新增“运行监控”导航，支持最近运行选择、节点延迟条形图与单次 Trace 瀑布列表；
- 遥测字段采用 allowlist，只允许任务 ID、节点、模型、计数和错误码，不保存案件全文、Prompt、API Key 与身份信息；
- 费用以人民币展示，单价由环境变量配置，默认 0 表示统计 Token 但不伪造费用；
- 运行数据写入现有 SQLite，不依赖第九周持久队列，也不改变已有 Agent 执行结果。

当前为本地可观测性实现：使用 OpenTelemetry SDK 生成 Span，同时持久化到 `agent_run_spans` 供页面查询；尚未接入 OTLP Collector、Prometheus 或告警平台。
